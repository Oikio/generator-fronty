// To export global variables use 'window.gScope.yourExport'
// Global scope:
// gScope = models and data
// gPlugins = own plugins
// gHelpers = global functions
//
// We use '.s-class' or 'data-*' to connect scripts with DOM except blocks which always have script dependencies.

'use strict';

$(function() {

});
